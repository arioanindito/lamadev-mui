import { createTheme } from "@mui/material";

export const theme = createTheme({
	palette: {
		primary: {
			main: "#00bcd4",
			contrastText: "#fff",
		},
		secondary: {
			main: "#ff9800",
			contrastText: "#fff",
		},
	},
});
